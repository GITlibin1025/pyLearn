Python的数据类型

转换类型
整型 int（）
浮点 float（）
字符串 str（）
布尔 bool
检验是什么类型用type （）函数


list-列表

列表删除元素
*<列表名称>.remove(<元素名称>)
*<列表名称>.pop（）没有参数默认删除最后一个并返回删除值

删除列表
del <列表名称>

切片
<列表名称>[数字索引：数字索引]	#包头不包尾

列表添加元素
<列表名称>.append('元素名称')

嵌套是二维数组
list1 =[1,2,[4,5,6],7,8]
要想得到4需要>>>list1[3][1]

内置参数用.
list1.count（元素名称）	是检查该元素出现多少次
list1.index（元素名称）/（元素名称,起始位置索引，结束位置索引）  查看该元素的索引

<列表名称>.reverse()		列表翻转
<列表名称>.sort()		从小到大排序
<列表名称>.sort(reverse = True)	从大到小排序



元组（tuple）是括号，逗号是关键


#格式化字符串需要学习
格式化整理
>>>"{0} love {1} {2}".format("I","li","bin")		#这个是位置索引，也可以用变量来代替
'I love li bin'

列表转换为元组用tuple（），把可迭代对象转化为元组

函数len（），返回长度

max（）返回序列或集合的最大值，同理有min（）

sum（），求和

sorted（），排序 从小到大

搜集参数
定义参数时在变量前加 *

lambda函数|匿名函数
格式 >>>lambda 变量 :  返回值公式






